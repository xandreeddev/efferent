---
title: Compression agent
description: Customizing context compression with a CompressionPolicy on the agent.
sidebar:
  label: Compression agent
  order: 5
---

import { Code } from '@astrojs/starlight/components';
import source from '../../../../../examples/compressionAgent.ts?raw';

Compression ("headroom") is a property of the agent — `AgentConfig.compression` — not a hardcoded loop
step. Omit it for the cache-safe default (`Headroom.default()`); set it to `Compression.none` to disable,
or compose a custom policy from `Compression.pipeline` / `Compression.when` and the `Headroom.*` tactics.
This example sets a conditional policy and exercises it with a tool that returns a large payload, so the
clip + reversible marker actually fire.

```sh
bun examples/compressionAgent.ts
```

<Code code={source} lang="ts" title="examples/compressionAgent.ts" />
