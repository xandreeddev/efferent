#!/usr/bin/env bash
# Dev launcher for the CODER — runs `efferent code` (the in-process focused
# coder subcommand) off the built bundle. Works from any workspace dir; rebuild
# after source edits with `bun run build`.
exec bun "$(cd "$(dirname "$0")/.." && pwd)/packages/cli/dist/efferent.js" code "$@"
