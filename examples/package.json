{
  "name": "@xandreed/examples",
  "version": "0.0.6",
  "private": true,
  "type": "module",
  "description": "Frozen examples of the previous-line SDK (kept as documentation for the published npm package; not built).",
  "scripts": {
    "dice": "bun diceAgent.ts",
    "calc": "bun calcAgent.ts",
    "file": "bun fileAgent.ts",
    "hooks": "bun hooksAgent.ts",
    "compression": "bun compressionAgent.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {}
}
