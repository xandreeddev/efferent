---
title: Plugin reference
description: Generated defaults, dependencies, and schemas for the first-party plugins.
---

Generated by `bun run docs:generate` from the plugin definitions.

## efferent/approval-host

Scope: **runtime** · API 1 · Version 0.2.0-next.0

Provides: `efferent/Approval`

Requires: none

### Defaults

```json
{}
```

### Configuration schema

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/schemas/%7B%7D",
  "anyOf": [
    {
      "type": "object"
    },
    {
      "type": "array"
    }
  ]
}
```

## @xandreed/plugin-session-sqlite

Scope: **runtime** · API 1 · Version 0.2.0-next.0

Provides: `efferent/SessionStore`

Requires: none

### Defaults

```json
{
  "path": ".efferent/runtime/sessions.db"
}
```

### Configuration schema

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": [
    "path"
  ],
  "properties": {
    "path": {
      "type": "string"
    }
  },
  "additionalProperties": false
}
```

## @xandreed/plugin-models

Scope: **session** · API 1 · Version 0.2.0-next.0

Provides: `@effect/ai/LanguageModel`, `@xandreed/core/UtilityLlm`, `@xandreed/core/AuthStore`, `@xandreed/core/SettingsStore`, `@xandreed/core/ModelCatalog`

Requires: `efferent/SessionEnvironment`

### Defaults

```json
{
  "model": "",
  "fastModel": "",
  "fallbackModel": "",
  "inheritPrevious": true
}
```

### Configuration schema

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": [
    "model",
    "fastModel",
    "fallbackModel",
    "inheritPrevious"
  ],
  "properties": {
    "model": {
      "type": "string"
    },
    "fastModel": {
      "type": "string"
    },
    "fallbackModel": {
      "type": "string"
    },
    "inheritPrevious": {
      "type": "boolean"
    }
  },
  "additionalProperties": false
}
```

## @xandreed/plugin-memory

Scope: **session** · API 1 · Version 0.2.0-next.0

Provides: `efferent/Memory`

Requires: `efferent/SessionEnvironment`

### Defaults

```json
{
  "file": ".efferent/runtime/memory.jsonl",
  "limit": 8
}
```

### Configuration schema

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$defs": {
    "Int": {
      "type": "integer",
      "description": "an integer",
      "title": "int"
    }
  },
  "type": "object",
  "required": [
    "file",
    "limit"
  ],
  "properties": {
    "file": {
      "type": "string"
    },
    "limit": {
      "$ref": "#/$defs/Int",
      "description": "a number between 1 and 100",
      "title": "between(1, 100)",
      "minimum": 1,
      "maximum": 100
    }
  },
  "additionalProperties": false
}
```

## @xandreed/plugin-context

Scope: **session** · API 1 · Version 0.2.0-next.0

Provides: `efferent/ContextManager`

Requires: `@xandreed/core/UtilityLlm`

### Defaults

```json
{
  "thresholdTokens": 80000,
  "keepTurns": 6
}
```

### Configuration schema

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$defs": {
    "Int": {
      "type": "integer",
      "description": "an integer",
      "title": "int"
    }
  },
  "type": "object",
  "required": [
    "thresholdTokens",
    "keepTurns"
  ],
  "properties": {
    "thresholdTokens": {
      "$ref": "#/$defs/Int",
      "description": "a positive number",
      "title": "positive",
      "exclusiveMinimum": 0
    },
    "keepTurns": {
      "$ref": "#/$defs/Int",
      "description": "a positive number",
      "title": "positive",
      "exclusiveMinimum": 0
    }
  },
  "additionalProperties": false
}
```

## @xandreed/plugin-policy-workspace

Scope: **session** · API 1 · Version 0.2.0-next.0

Provides: `efferent/ActionPolicy`

Requires: `efferent/SessionEnvironment`, `efferent/Approval`

### Defaults

```json
{}
```

### Configuration schema

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/schemas/%7B%7D",
  "anyOf": [
    {
      "type": "object"
    },
    {
      "type": "array"
    }
  ]
}
```

## @xandreed/plugin-mcp

Scope: **session** · API 1 · Version 0.2.0-next.0

Provides: `@xandreed/core/McpClient`

Requires: `efferent/SessionEnvironment`, `efferent/ActionPolicy`

### Defaults

```json
{
  "servers": {}
}
```

### Configuration schema

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": [
    "servers"
  ],
  "properties": {
    "servers": {
      "type": "object",
      "required": [],
      "properties": {},
      "additionalProperties": {
        "type": "object",
        "required": [
          "command"
        ],
        "properties": {
          "command": {
            "type": "string"
          },
          "args": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "env": {
            "type": "object",
            "required": [],
            "properties": {},
            "additionalProperties": {
              "type": "string"
            }
          }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
```

## @xandreed/plugin-tools-local

Scope: **session** · API 1 · Version 0.2.0-next.0

Provides: `efferent/AgentTools`

Requires: `efferent/SessionEnvironment`, `efferent/ActionPolicy`, `efferent/Memory`, `@xandreed/core/McpClient`

### Defaults

```json
{
  "readOnly": false
}
```

### Configuration schema

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": [
    "readOnly"
  ],
  "properties": {
    "readOnly": {
      "type": "boolean"
    }
  },
  "additionalProperties": false
}
```

## @xandreed/plugin-agent-loop

Scope: **session** · API 1 · Version 0.2.0-next.0

Provides: `efferent/AgentLoop`

Requires: `@effect/ai/LanguageModel`, `efferent/AgentTools`, `efferent/SessionStore`, `efferent/Memory`, `efferent/ContextManager`

### Defaults

```json
{
  "maxSteps": 100,
  "toolConcurrency": 1,
  "streaming": true
}
```

### Configuration schema

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$defs": {
    "Int": {
      "type": "integer",
      "description": "an integer",
      "title": "int"
    }
  },
  "type": "object",
  "required": [
    "maxSteps",
    "toolConcurrency",
    "streaming"
  ],
  "properties": {
    "maxSteps": {
      "$ref": "#/$defs/Int",
      "description": "a number between 1 and 1000",
      "title": "between(1, 1000)",
      "minimum": 1,
      "maximum": 1000
    },
    "toolConcurrency": {
      "$ref": "#/$defs/Int",
      "description": "a number between 1 and 32",
      "title": "between(1, 32)",
      "minimum": 1,
      "maximum": 32
    },
    "streaming": {
      "type": "boolean"
    }
  },
  "additionalProperties": false
}
```

## @xandreed/plugin-telemetry

Scope: **session** · API 1 · Version 0.2.0-next.0

Provides: `efferent/TurnHooks`

Requires: none

### Defaults

```json
{
  "enabled": false,
  "serviceName": "efferent"
}
```

### Configuration schema

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": [
    "enabled",
    "serviceName"
  ],
  "properties": {
    "enabled": {
      "type": "boolean"
    },
    "serviceName": {
      "type": "string"
    }
  },
  "additionalProperties": false
}
```

## @xandreed/plugin-agent-loop/delegate

Scope: **session** · API 1 · Version 0.2.0-next.0

Provides: `efferent/DelegateLoop`

Requires: `@effect/ai/LanguageModel`, `efferent/AgentTools`, `efferent/SessionStore`, `efferent/Memory`, `efferent/ContextManager`

### Defaults

```json
{
  "maxSteps": 100,
  "toolConcurrency": 1,
  "streaming": true
}
```

### Configuration schema

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$defs": {
    "Int": {
      "type": "integer",
      "description": "an integer",
      "title": "int"
    }
  },
  "type": "object",
  "required": [
    "maxSteps",
    "toolConcurrency",
    "streaming"
  ],
  "properties": {
    "maxSteps": {
      "$ref": "#/$defs/Int",
      "description": "a number between 1 and 1000",
      "title": "between(1, 1000)",
      "minimum": 1,
      "maximum": 1000
    },
    "toolConcurrency": {
      "$ref": "#/$defs/Int",
      "description": "a number between 1 and 32",
      "title": "between(1, 32)",
      "minimum": 1,
      "maximum": 32
    },
    "streaming": {
      "type": "boolean"
    }
  },
  "additionalProperties": false
}
```

## @xandreed/smith/workflow

Scope: **session** · API 1 · Version 0.2.0-next.0

Provides: `efferent/AgentLoop`

Requires: `efferent/DelegateLoop`, `efferent/SessionStore`, `efferent/Approval`

### Defaults

```json
{
  "mode": "spec",
  "maxAttempts": 3,
  "budgetMillis": 900000,
  "testCommand": "",
  "configPath": ""
}
```

### Configuration schema

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$defs": {
    "Int": {
      "type": "integer",
      "description": "an integer",
      "title": "int"
    },
    "Positive": {
      "type": "number",
      "description": "a positive number",
      "title": "positive",
      "exclusiveMinimum": 0
    }
  },
  "type": "object",
  "required": [
    "mode",
    "maxAttempts",
    "budgetMillis",
    "testCommand",
    "configPath"
  ],
  "properties": {
    "mode": {
      "type": "string",
      "enum": [
        "spec",
        "lock",
        "forge"
      ]
    },
    "maxAttempts": {
      "$ref": "#/$defs/Int",
      "description": "a number between 1 and 10",
      "title": "between(1, 10)",
      "minimum": 1,
      "maximum": 10
    },
    "budgetMillis": {
      "$ref": "#/$defs/Positive"
    },
    "testCommand": {
      "type": "string"
    },
    "configPath": {
      "type": "string"
    }
  },
  "additionalProperties": false
}
```
