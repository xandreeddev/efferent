# House rules
- Errors are values: no try/catch/throw — use Effect or plain returns.
- State is a fold: no `let`, no loop statements — Array combinators.
- Absence is `Option` — never return `A | undefined`.
- Match the style of src/greeting.ts.
