# efferent — coding agent on Effect.ts + Bun

Coding agent CLI built in public as `@xandreeddev`. See `../CLAUDE.md` (parent tree) for the broader project rules — alias identity, OPSEC, weekly cadence, locked stack. See `docs/roadmap.md` for what's deferred and `docs/models.md` for the exhaustive model-usage map (every LLM call site, its selection, where the spend lands).

## Architecture (ports & adapters)

```
packages/
├── core/         pure domain: entities, ports, use cases, prompts — depends on `effect` + `@effect/ai`
├── adapters/     Layer impls of ports — depends on @xandreed/sdk-core + external SDKs (@effect/ai-google, @effect/ai-openai, Postgres)
└── cli/          coding-agent driver: TUI + print + json + rpc modes
```

**Dependency direction is strictly inward.** `cli` → `adapters` → `core`. `core` imports nothing from siblings. `adapters` imports `@xandreed/sdk-core` + the external SDK it wraps. Drivers compose the layers at the very edge and hand off to `BunRuntime.runMain`.

## Conventions

- **Ports** are `Context.Tag` services in `@xandreed/sdk-core/ports/`. Each ships its tagged errors next to it.
- **Adapters** provide one `Layer.effect` per port. External promises go through `Effect.tryPromise`, mapped into the port's tagged error. Config via `Config.string` — never hardcode.
- **Use cases** live in `@xandreed/sdk-core/usecases/` returning `Effect.Effect<A, E, …>`. No IO; the only SDK allowed in `core` is `@effect/ai` (provider-agnostic — `LanguageModel`, `Tool`, `Toolkit`, `Prompt`). Provider packages (`@effect/ai-google`, `@effect/ai-openai`) live in `adapters`.
- **Agent configs** (`@xandreed/sdk-core/usecases/{agentConfig,coderAgentConfig}.ts`) bundle a system prompt + an `@effect/ai` `Toolkit` into an `AgentConfig<Tools>`. `runAgent` is parameterized by config; the CLI picks `coderAgentConfig(cwd)`. The toolkit's handler `Layer` (`codingToolkitLayer(cwd, skills, { allowBash })`) is provided at the driver's composition root — it carries the runtime deps (`cwd`, `FileSystem`, `Shell`).
- **Schema** lives in `effect` itself: `import { Schema } from "effect"`.
- Bun runs `.ts` directly. No build step, no emit. `tsc --noEmit` is purely a typecheck gate.
- **No `try/catch`, `throw`, or `.catch()` in `@xandreed/sdk-core/src/`.** Error handling uses Effect's typed errors (`Effect.fail`, `Effect.die`, `Effect.catchAll`, `Effect.catchTag`). Enforced by `scripts/banTryCatch.ts` — a zero-dep TypeScript-AST scan (so `Effect.try`/`Effect.tryPromise`/strings/comments never false-positive), folded into `bun run typecheck`.
  - **After the task is complete, run `bun run typecheck` to validate the ban.** It runs tsc + the AST scan; a banned construct fails the command and the change is rejected.
  - Enforced at three layers: a local `pre-commit` hook (installed by `bun install` via the `prepare` script → `scripts/installHooks.ts`; `--no-verify` bypass discouraged), the `ci` workflow on every push/PR (`.github/workflows/ci.yml`), and the release gate. Branch protection on `main` requires `ci`, so a violation can't be merged or pushed.
- File naming: camelCase for files that export functions; PascalCase for files that export types / `Context.Tag` classes.

## Dev commands

```bash
bun install                                          # from agent/ root only
bun run typecheck                                    # cross-package via path mappings
bun run build                                        # bundle the CLI → packages/code/dist/efferent.js
bun run eval [name …]                                # run eval suites (key-gated)
bun run generate-models                              # refresh the model catalogue from models.dev

# Postgres is OPTIONAL (only when EFFERENT_DB_URL is set); the default store is SQLite.
docker compose up -d                                 # start local Postgres (host port 5434)
docker compose down                                  # stop it
docker compose exec postgres psql -U agent -d agent  # poke at the DB

bun run start:fresh                                  # hermetic blank run: EFFERENT_HOME=$(mktemp -d) — onboarding from scratch, touches neither ~/.efferent nor the repo
efferent                                                # full TUI (TTY default; :login to add a provider)
efferent "<prompt>"                                     # one-shot print mode
efferent -p / --print                                   # explicit print mode (stdin OK with "-")
efferent --mode json "<prompt>"                         # stream events as JSONL on stdout
efferent --mode rpc                                     # bidirectional JSON-RPC on stdin/stdout
efferent --resume <conversationId>                      # resume an existing session
efferent --allow-bash                                   # allow bash in non-interactive modes
efferent --cwd <path>                                   # override workspace (defaults to process.cwd())
```

Credentials & setup. **Credentials come *only* from `~/.efferent/auth.json`, written by the in-app `:login` flow — there is no env-var key reading and no `init` command.** `efferent` always boots straight into the TUI; with no credential it shows `No models available. Run :login …` and sending a message short-circuits to the same hint. **`:login`** is an in-app flow (`packages/code/src/cli/presentation/loginFlow.ts` + `promptBox.ts`, composited like the `:model` picker): pick *Use a subscription* (OAuth) or *Use an API key* → pick a provider (status-tagged ✓api key / ✓subscription / •unconfigured) → paste a masked key **or** run the OAuth flow → persisted to `auth.json` → **usable that turn, no restart**. **`:logout <provider>`** removes a credential. The first successful login also pins that provider's default model. Non-interactive modes (print/json/rpc) can't run `:login`, so they require a credential already in `auth.json` (else they exit with a hint).
- **`AuthStore`** port (`@xandreed/sdk-core/ports/AuthStore.ts`) + `LocalAuthStoreLive` (`adapters/src/auth/local.ts`): per-provider creds (`{ type: "api_key" } | { type: "oauth", … } | { type: "local", baseUrl? }`; legacy flat-string entries read as api keys), atomic `0600` writes. `resolveKey` is read lazily per request by the router/registry, refreshing a near-expiry OAuth token first. Reads no env. **Config/auth scoping** (shared with `SettingsStore`, resolved by `adapters/src/configRoots.ts`): **`EFFERENT_HOME` set ⇒ that one dir is the entire world** — `$EFFERENT_HOME/.efferent/{auth.json,config.json}`, no `~/.efferent`, no cwd, no merge (the hermetic sandbox / `start:fresh`). **Unset ⇒ two tiers** — global `~/.efferent` + local `<cwd>/.efferent`, merged **local-over-global**; onboarding picks the scope (step 1), `:login` defaults global, `:set`/`/model`/`:theme` default local, and a local write seeds a `.efferent/.gitignore` (`auth.json`, `config.json`, `*.db*` — `skills/` stays committable). The router builds the provider client per turn from `resolveKey`, so a login mid-session takes effect immediately. Evals/CI use `EnvAuthStoreLive` (`adapters/src/auth/env.ts`) — the *only* place provider key env vars are read.
- `EFFERENT_DB_URL` — **optional.** Selects the conversation store: `postgres://…` → Postgres; any other value → SQLite at that path (optionally `sqlite:`-prefixed); unset → zero-config **SQLite** at `~/.efferent/efferent.db` (the default; no Docker). May also live as `dbUrl` in `~/.efferent/config.json`; the env var **overrides** the config value (seeded into the env at boot by `seedDbUrlFromConfig`, since the store is chosen at layer-build before settings load). Parsed by `parseDbTarget` and selected in `adapters/src/database/migrator.ts` (`ConversationStoreLive`); migrations load via `Migrator.fromRecord` (bundle-safe).
- Optional `EFFERENT_MODEL` — seeds the active model when no `config.json` pins one. Accepts `"<provider>:<modelId>"` (e.g. `openai:gpt-4o`) or a bare id (provider inferred; defaults to Google). Default `google:gemini-3.5-flash`. An explicit `/model` switch is persisted to `.efferent/config.json` and wins over `EFFERENT_MODEL`.

## Install / distribution

Published as the unscoped npm package **`efferent`** (bin `efferent` / `eff`), a Bun bundle (`bun run build` → `packages/code/dist/efferent.js`, core + adapters + `@effect/*` + `@opentui/solid` + `solid-js` inlined, `#!/usr/bin/env bun`). Requires **Bun** at runtime and has **two runtime dependencies: `@opentui/core`** (+ its platform-native `@opentui/core-<platform>` subpackage) — the TUI's native Zig renderer is `dlopen`'d via FFI and can't be inlined, so it stays external; it *also* ships the tree-sitter grammar WASM (`assets/{javascript,typescript,markdown,zig}`) + the `parser.worker.js`, both resolved from its own install dir — **and `web-tree-sitter`** (the highlighter that worker imports; a peer dep of `@opentui/core`, so we declare it directly or `npm i -g` won't install it). Everything else is inlined; the workspace + the other `@effect/*` are devDependencies. `npm i -g efferent` then just run `efferent` — it boots into the TUI and you add a provider in-session with **`:login`** (subscription/OAuth or API key); no wizard, no `init`, no env vars. Works in any project with no Docker (SQLite history + `auth.json` under `~/.efferent/`). **Live on npm since 2026-06-12: `efferent@0.1.0`**, publisher `xandreed` (the alias npm account). Releases are automated: bump `packages/code` version → push → `gh release create vX.Y.Z` → `.github/workflows/release.yml` gates on typecheck + tests + a tag-matches-version check + a bundle leak audit (no build-machine paths), then `npm publish --provenance` via npm **trusted publishing** (OIDC — configured on npmjs.com for this repo+workflow, no token secret). The build stubs the provider tokenizer packages out of the bundle (`scripts/build.ts` — they baked the build machine's absolute path into the artifact and would crash a published install at module-init; efferent never tokenizes locally). Cutting a release is outward-facing — only on explicit sign-off.

## Coding tools (CLI)

An `@effect/ai` `Toolkit` in `@xandreed/sdk-core/usecases/codingToolkit.ts`, backed by the `FileSystem` and `Shell` ports. Each tool is a `Tool.make` def with an **object** `success` Struct + a shared `failure` Struct and `failureMode: "return"` (so a tool failure is returned to the model as data, not thrown). Handlers live in `codingToolkitLayer(cwd, skills, { allowBash })`, which resolves `FileSystem`/`Shell` from context at layer-build time. (Gemini rules learned the hard way: every tool needs ≥1 parameter, and `success` must be an object — see the spike notes in the plan.)

| tool         | parameters                                       | implementation                              |
|--------------|--------------------------------------------------|---------------------------------------------|
| `read_file`  | `{ path; offset?; limit? }`                      | `FileSystem.read`                            |
| `write_file` | `{ path; content }`                              | `FileSystem.write`                           |
| `edit_file`  | `{ path; edits: [{ oldText; newText }] }` *or* flat `{ path; oldText; newText }` | `normalizeEdits` → read → string replace → write, returns diff |
| `Bash`       | `{ command; timeout? }`                          | `Shell.exec`, cwd bound at tool-build time  |
| `grep`       | `{ pattern; dir?; flags?; context? }`            | shells out to `grep -rnE` for now            |
| `glob`       | `{ pattern; dir? }`                              | `FileSystem.glob` via `Bun.Glob`             |
| `ls`         | `{ path?; recursive? }`                          | `FileSystem.list`                            |
| `web_fetch`  | `{ url; maxBytes? }`                             | `Http.get`; HTML reduced to text             |
| `search_web` | `{ query }`                                      | `WebSearch.search` → `{ answer; sources }`   |
| `run_agent`  | `{ name; folder; task; seedFromNode?; seedMode?; agent? }` | spawns a folder-scoped sub-agent **in the background** and returns `{ nodeId; name; status: "running" }` immediately — non-blocking (see Sub-agents & context tree); `name` (2-5 words) is its display title everywhere |
| `wait_for_agents` | `{ nodeIds?; timeoutSeconds? }`             | the non-blocking **gather**: parks the caller (interruptibly) until a watched agent finishes / a message lands in the caller's inbox / the timeout, then returns each agent's status (+ finished summaries/files), the caller's drained inbox, and the blackboard tail. `AgentBus.awaitChange` |
| `update_plan` | `{ steps: [{ step; status }] }`                  | the agent's working plan (statuses pending/active/done); each call replaces it whole — pure echo handler, the UI reads the args |

All paths resolved relative to the `cwd` bound in `codingToolkitLayer(cwd)`. **`edit_file` accepts two shapes:** the canonical `edits: [{ oldText, newText }]` array, or a flat single-edit `{ oldText, newText }` at the top level — the shape models trained on Claude Code's `Edit` tool emit when there's only one edit. Both are normalised by `normalizeEdits` *inside* the handler. This matters because `@effect/ai` decodes tool-call parameters **before** the handler runs (`Toolkit.handle`), so a shape mismatch is an `AiError.MalformedOutput` that `failureMode: "return"` can't catch — it aborts the whole turn. Keeping the parameter schema permissive enough to decode both shapes, then validating in the handler, turns "wrong shape" into a graceful, model-visible `EditFailed` instead of a dead turn. **Names matter:** Anthropic reserves the lowercase tool names `bash` / `web_search` / `computer` for its provider-defined tools (case-sensitive lookup); when we register a handler-backed tool by one of those names, the Anthropic SDK reroutes it to the built-in provider tool — which isn't in our toolkit, so the turn fails. So our shell is **`Bash`** (capital B) and our search is **`search_web`** (reversed). `search_web` is provider-native (see below) — it has no parameter-less constraint issue because `{ query }` satisfies the ≥1-parameter rule.

## Web search

Provider-native, no extra key. The `web_search` tool is backed by the `WebSearch` port (`@xandreed/sdk-core/ports/WebSearch.ts`) and `WebSearchLive` (`adapters/src/llm/webSearch.ts`). Each call is a **dedicated, grounding-only** `generateText` against a provider's *server-side* search tool — Gemini `GoogleTool.GoogleSearch` or OpenAI `OpenAiTool.WebSearch` (`Tool.ProviderDefined`, handler-free). It returns `{ answer, sources }` (sources from the response's `UrlSourcePart`s). The model finds with `web_search`, then reads a chosen source with `web_fetch`.

**Deliberately a separate thing** (not merged into the agent's main toolkit): the search call carries *only* the search tool, never the agent's function tools — so it needs no extra credential beyond a logged-in provider, and sidesteps providers (notably Gemini) that won't combine grounding with function calling in one request. It's also decoupled from the chat `/model`: configured via `EFFERENT_SEARCH_MODEL` (`<provider>:<modelId>`), else defaults to whichever of Google/OpenAI is logged in (Google preferred). `WebSearchLive` resolves its key per call from the `AuthStore` and builds its own client over a `FetchHttpClient`. Gemini's grounding source URLs are `vertexaisearch…` redirects — `web_fetch` follows them.

## Agent loop

One use case — **`runAgent(config, conversationId, prompt, hooks?)`** in `@xandreed/sdk-core/usecases/runAgent.ts` — drives the whole interaction. The loop lives in `@xandreed/sdk-core/usecases/agentLoop.ts`. **`@effect/ai` resolves a single model step's tool calls (its handlers are our Effects) but does NOT iterate across turns — so iteration is ours**: each turn maps the message buffer to a `Prompt`, calls `LanguageModel.generateText({ prompt, toolkit })`, appends the response parts as the new tail, and re-invokes until `finishReason !== "tool-calls"` or `maxSteps`.

```
efferent <prompt>
  ↓
runAgent(coderAgentConfig(cwd), conversationId, prompt, hooks)
  ├── ConversationStore.append(user) ; list → message history
  ├── runAgentLoop({ system, messages, toolkit, hooks, maxSteps })
  │     ├── turn 0..N: onTransformContext? → onTurnStart
  │     │              → Prompt.make([system, ...toPromptMessages(messages)])
  │     │              → LanguageModel.generateText({ prompt, toolkit })   (resolves this step's tools)
  │     │              → responseToAgentMessages(res.content) onto buffer  (carries thought_signature in providerOptions)
  │     │              → re-emit onBeforeToolCall / onAfterToolCall / onAssistantMessage(TokenUsage) from the response
  │     │              → continue iff finishReason === "tool-calls" && toolCalls > 0 ; onShouldStopAfterTurn?
  │     └── while turnIndex < maxSteps
  ├── ConversationStore.append(tail)
  └── return AgentResult { finalText, messages }
```

**Prompt mapping** (`@xandreed/sdk-core/usecases/promptMapping.ts`): bridges our persisted `AgentMessage` (our stable shape, unchanged) with `@effect/ai`'s `Prompt`/`Response`. The opaque provider blob is carried verbatim both ways (`providerOptions ↔ options`/`metadata`), which is how Gemini's `thought_signature` round-trips across our turns — `Prompt.fromResponseParts` drops it, so we map by hand.

**Hooks** (`@xandreed/sdk-core/AgentHooks`): the loop re-emits the legacy event vocabulary (`onTurnStart` / tool events / `onAssistantMessage`) from each resolved response, so the CLI's `makeEventHooks(queue)` (`packages/code/src/events.ts`) and the TUI execution tree keep working unchanged.

**Graceful tool errors**: each tool's `failureMode: "return"` + `failure` Struct means a handler failure (e.g. `FileNotFound`, ambiguous edit) is returned to the model as a tool result instead of aborting the turn. **But `failureMode` only catches *handler* failures** — `@effect/ai` decodes a tool call's parameters (and resolves the tool name) *inside* `Toolkit.handle`, **before** the handler runs, so a wrong-shaped call or a hallucinated tool name fails with `AiError.MalformedOutput`, which `failureMode` never sees and which aborts the whole turn. `recoverMalformedToolCalls` (`agentLoop.ts`) closes that gap: it resolves the toolkit's handler once and wraps it so a `MalformedOutput` is converted into an ordinary tool *result* (`isFailure: true`, `error: "InvalidToolCall"`). The assistant tool-call ↔ tool-result pairing stays valid, the loop proceeds to its next turn, and the model reads the decode error from context and emits a corrected call — the **same** recovery path as any returned failure, no retry machinery. (`MalformedInput` — a result encode/validate failure, i.e. *our* bug — is deliberately let through, not masked.)

**Bash safety**: three layers. The static `allowBash` flag (denied → returned as a tool failure; non-interactive modes pass `--allow-bash`), then the **`Approval` port** (`@xandreed/sdk-core/ports/Approval.ts`) consulted per command, fronted by the **fast-tier auto-approval judge**. An unmatched command first goes to the judge (`judgeApproval` in `core/usecases/autoApproval.ts`, `UtilityLlm` on the **fast** role; `:set autoApprove off` disables, default ON): it classifies the input against the **permitted folders** — the workspace root (opening efferent in a cwd is the standing grant) plus every folder granted before — and either **allows silently** (ordinary development work whose paths stay inside; a dim `fast approved: …` info line in the rail, spend → `byRole.fast`) or **prompts**, the modal carrying its one-line reason. Not a security boundary (the scope sandbox owns that) — a prompt-fatigue killer: the judge can only remove prompts, never a human's view of anything it isn't sure about, and any error (no key, bad JSON, 429) degrades to the modal. The TUI modal (`cli/approval.ts`, `a/s/p/d`): **allow once / allow for session / always allow in this project / deny with a typed reason**. Permission is **path-based**: when the judge named the out-of-bounds folder, session/project answers grant *that folder* (session Ref / `Settings.approvedFolders`) and future commands inside it pass the judge; with no folder hint they grant the command *rule* as before (`bashRuleKey`: command+subcommand, exact-match when shell metacharacters are present — persisted to `Settings.approvedBashRules`). A denial (with its reason) returns to the model as an ordinary tool failure, so it adjusts in the same turn. Headless modes + evals provide the static allow-all impl behind the `--allow-bash` gate — CI never prompts and never judges.

## Sub-agents & context tree

Sub-agent spawning is **one generic tool over a persistent branching context tree**, and it is **asynchronous** — the fleet runs like a team of people, not a blocking call tree. (`buildScopeRuntime.ts` + the unified `agentBus.ts` live in `packages/code/src/usecases/`; the loop primitives they call — `runAgentLoop`, `RunContextRef`, `tokenBudget` — are in `@xandreed/sdk-core`.)

- **`run_agent({ name, folder, task, seedFromNode?, seedMode?, agent? })`**: spawns a sub-agent scoped to `folder` (writes + bash confined via `ScopeBinding{rootDir, enforceWrite}` — the sandbox machinery in `codingToolkit.ts`, unchanged) and **forks it as a supervised background fiber** (`Effect.forkDaemon`, registered on the bus), returning `{ nodeId, name, status: "running" }` **immediately** — the caller never blocks on the subtree (that was the "parent hangs while the fleet works" bug). `failureMode: "return"` so a failed spawn is data, not a dead turn. The toolkit is **static** (`genericToolkit` = base tools + comms + `run_agent` + `wait_for_agents`) — the same toolkit at every depth; sub-agents can spawn sub-agents.
- **Gather, don't block** (`wait_for_agents`, `AgentBus.awaitChange`): a spawner collects results with `wait_for_agents({ nodeIds?, timeoutSeconds? })`, which parks **only the caller's fiber**, interruptibly, until a watched agent finishes, a message lands in the caller's inbox, or the timeout — then returns each agent's status (+ finished summaries/files), the caller's drained inbox, and the blackboard tail. No agent ever blocks another; loop it until `allDone`. When a run finishes, the bus also drops a completion line in its **parent's mailbox** + the blackboard and keeps a terminal result record, so a parent that wasn't waiting still picks it up at its next turn boundary.
- **The bus is the live layer** (`agentBus.ts`, `AgentBus`): per-agent **mailboxes** (keyed by node id; the root conversation registers one under its `conversationId`, so the human can message the lead mid-turn), a shared **blackboard**, plus **supervision** — each running agent's fiber (so `:stop`/Esc/teardown interrupt it via `interruptAll`), a completion `Deferred` (so a parent awaits without polling), parent→child links (`childrenOf`, running + finished), and a `snapshot`. The durable record stays the context tree; this is the live layer the TUI + agents tap (`:fleet`, `:tree`, the header `◆ N agents` chip, a node preview).
- **The built-in team** (`usecases/teamAgents.ts`, merged by `withBuiltinAgents`): the generic root delegates real coding to a **`coordinator`** (leads — `run_agent` + `wait_for_agents` + comms, no writes), which staffs the specialists the work needs — **`frontend` / `backend` / `qa` / `product` / `implementer`** (leaf coders + comms) — and validates each piece with the read-only **`architect`** (fresh context, SOUND/NEEDS WORK/BLOCKED) before delivering. A workspace `.efferent/agents/<name>.md` overrides any built-in.
- **Persistence** (`ContextTreeStore` port, `@xandreed/sdk-core/ports/ContextTreeStore.ts`; SQLite migration `0002` / pg `0006`, tables `context_nodes` + `context_messages`): every spawn is an `AgentContextNode` `{ parentId, rootConversationId, edgeKind, folder, seed, status, returnSummary, filesChanged, usage }`. The node's full message history persists; the `seed` column is a small **descriptor** (`task` | `selection` | `handoff`) — actual seed messages live only in `context_messages`.
- **Resume / branch / handoff**: `seedFromNode` + `seedMode: "resume"` appends the task to the existing node and continues in its context (same node grows — full history every turn); `"branch"` spawns a **child** node (`edgeKind: "branched"`) seeded from the source node's verbatim messages; `"handoff"` (prompted as the PREFERRED follow-up mode) spawns a child seeded with a **generated brief** of the source's work (`generateHandoffBrief` → `handoffToMessage`, seed kind `handoff`) — continuity at a fraction of the tokens. The routing policy lives in the tool description + `# Sub-agents` prompt section: one agent = one piece of work, fresh spawn by default, reuse only for direct follow-ups, cheapest sufficient seed. The tree records how context branched and evolved.
- **Identity & bounds**: `RunContextRef` (a `FiberRef` in `usecases/runContext.ts`) carries `{ rootConversationId, parentNodeId, depth, tokenPool }` ambiently — `runAgent` seeds it via `Effect.locally`, each spawn re-seeds for its children. A `maxDepth` guard (default 2 — the root agent can spawn sub-agents, and those can spawn one more level) returns `MaxDepthReached` as a tool failure; each sub-agent run is bounded by its own step cap (default 80, `Settings.subAgentMaxSteps`, `:set subAgentMaxSteps <n>`, threaded live through `RunContext` like the pool). A capped run's summary is stamped `[stopped early: the step limit was reached — this result is partial]` (mirroring the budget stop), so the parent reads truncation instead of trusting a mid-thought sentence as the deliverable.
- **Token budget** (`usecases/tokenBudget.ts`): depth bounds termination, the **shared pool** bounds spend — one `Ref` (default 1M billed tokens, `Settings.subAgentTokenBudget`, `:set subAgentTokenBudget <n>`, 0 = off) drawn from by every sub-agent in a turn's subtree. A drained pool refuses new spawns (`BudgetExhausted`, model-readable) and stops running sub-agents at their next turn boundary, marking the summary `[stopped early …]` so partial results read as partial. `:tree` rows show each node's billed tokens.
- **Parallel fan-out**: spawning is non-blocking, so multiple `run_agent` calls in one assistant turn each fork-and-return at once — the whole fleet runs concurrently (a turn's tool calls themselves resolve with `generateText`'s `concurrency`, default 4 — `DEFAULT_TOOL_CONCURRENCY` in `agentLoop.ts`). Disjoint folders are write-safe by construction (the sandbox); same-folder spawns serialize on a per-folder semaphore (`usecases/folderLock.ts`, one locks map per `ScopeRuntime` threaded through nested spawns). Because the fleet are background daemons (they outlive the spawning turn), teardown is explicit: **Esc** interrupts the root fiber AND `bus.interruptAll()`, and a runtime finalizer does the same on exit — structured cancellation, no orphans. The Activity pane attributes parallel runs correctly: inner tool/assistant events carry `subAgentNodeId` (stamped by `makeInnerHooks`), `subagent_start` carries `parentNodeId`, and the pump keys each run to its own tree container (`onSubAgentStartKeyed`/`onToolStartUnder` in `executionTree.ts`) instead of the old single-open-stack model that nested parallel runs into a staircase. The events flow through the pump even for background daemons, so `:tree`/activity/the fleet chip update live after the root turn ends.
- **Staleness** (`usecases/staleness.ts`): a persisted context is a cache of a world that keeps changing. Nodes are stamped with the workspace git HEAD at finish (`workspaceRef`); resume/branch against a moved HEAD prepends a **staleness brief** (ref range + `git diff --stat` scoped to the node's folder + "re-read before editing") and `:tree` shows a yellow `stale` badge. All best-effort: non-git workspaces never stamp, a GC'd ref never breaks a spawn.
- **`SCOPE.md` is ambient folder context, not an agent definition**: `getScopePromptBody(folder)` (`discoverScopeTree.ts`) reads `folder/SCOPE.md` (frontmatter stripped) and injects the body into any sub-agent scoped there. Frontmatter `name`/`description` are inert metadata now; the old `delegate_to_<name>` tools are gone.
- **Events**: `onSubAgentStart/End` carry an optional `nodeId`, bridging the ephemeral per-run execution tree and the persistent context tree. In the TUI rail, a `run_agent` call renders as the sub-agent container itself (Task-style), not a redundant tool pill.
- **TUI** — the right pane is ONE always-visible **fleet tree** (`:tree`, `FleetTree` → `ContextTreeView` + a reactive **detail section** below, `view/panes/side/NodeDetail.tsx`, following the cursor — full unclipped summary/seed/files/billed tokens, a running node's **live tool feed** via `TreeNode.nodeId` + `findByNodeId`). It is **current-session-only** and **always expanded**: a single **non-foldable root** — the active session (its orchestrator shows a live `●` while its own turn runs) — with its coordinator/agents/sub-agents nested beneath as a **git-graph-railed tree** (`├─`/`└─` + `│` continuation, computed purely in `buildNavRows`; a `branched` connector is accent-tinted), each node a status glyph (`●`/`✓`/`✗`), provenance (`spawned`/`branched`/`resumed`), seed kind, files count, return summary, billed tokens. Loaders (`loadAgentTree` / `loadSessions` / `refreshNav` in `actions/contextTree.ts`, **`activeOnly` by default**) seed it at boot and refresh each turn end + on every sub-agent start/end — so the fleet appears and flips status live (the in-process `code` driver AND the remote `efferent` daemon path, which reads the shared store via `refreshNav` in `remoteRuntime.ts`). Other sessions are reached via the **`:browse`/resume** picker, not this pane — **no four-view cycle, no `sessions`/`fleets` tabs** (that model is gone). **`↵`** opens the row: a conversation **becomes the active session** (`switchToConversation` — resume + reload, refused mid-turn); an agent node opens a read-only **session preview** overlaying the conversation pane (`NodePreview` in `state/conversation.ts` — `viewBlocks = preview ?? blocks`, so the event pump keeps writing to the live rail underneath; title `agent: <folder>`, `── seed … ──`/`── run starts ──` boundary rules from `seedMessageCount`; `↵`-again/`q`/idle-`Esc` close). **While a preview is open the composer routes to that agent — like pairing with a teammate** (input title `input → agent: <folder>`), and it never freezes: if the node is **running**, the message is delivered to its **bus mailbox** (`bus.post` — it reads it at its next step, the composer stays free, no busy flip); if it's **finished**, the message **resumes the node in place** via `ScopeRuntime.resumeNode` (the human-driven `seedMode:"resume"` — staleness brief, depth-0 fresh budget, children hang off the node; refused while another turn runs). The preview re-fetches at turn end (`{focus:false}` refresh keeps the composer). **Routing is explicit: with a preview open the composer pairs with that agent (above); with no preview open, a message typed while the root is busy QUEUES (agy-style — a pending `▸` entry that runs as the next turn when the current finishes), NOT injected mid-turn.** The in-process `code` driver queues locally (`submit.ts` → `run.enqueue`, drained in `finishTurn`); the remote `efferent` driver queues on the daemon (`inProcess.send` → run-state queue, surfaced in `SessionState.queue`, mirrored into the pending list by `remoteRuntime`'s `refreshQueue` — which also surfaces a drained prompt as its rail user line, since the daemon emits no user event). (Sub-agent *results* still auto-deliver to the root via its mailbox + `onTransformContext` — a separate path.) **`c`** forks an agent node's full context into a new conversation and makes it active (the human-driven branch — `continueFromNode`); `d` drops a node + descendants (nodes only, running guarded); refreshed when a run finishes spawning (`presentation/contextTreeView.ts`, `presentation/nodePreview.ts`, `view/panes/side/ContextTree.tsx`, `actions/contextTree.ts`, `actions/submit.ts:submitToNode`). Switching focus (Tab / Ctrl-hjkl) never reorders the panes.

## Handoff & conversation context

A **handoff** replaces the *loaded* history with a model-generated summary while keeping the originals. It's how a long session frees context without losing the record.

- **Storage** (`checkpoints` table, migration `0005`): a checkpoint is `{ messagePosition, summary }` for a conversation. Original `messages` rows are never modified. `ConversationStore` exposes `checkpoint(id, summary)` (atomic — folds at `COALESCE(MAX(position), -1)` in one statement), `getLatestCheckpoint`, `listCheckpoints`, `list` (ALL messages, for browsing), and `listActive` (only real rows **after** the latest fold).
- **Loading** (`runAgent`): loads `getLatestCheckpoint` + `listActive`, prepending the summary as a single synthetic `user` message via `handoffToMessage` (`promptMapping.ts`) — domain logic stays in core, not the adapter. So the model sees `[handoff summary] + [messages since the fold] + [new user msg]`; everything before the fold is preserved but never re-fed.
- **Creating** (`createHandoff`, `@xandreed/sdk-core/usecases/handoff.ts`): summarizes the **currently loaded view** (prior summary + active window, never the raw `list`) with `HANDOFF_PROMPT` (`prompts/handoff.ts`, a Goal/State/Next-steps/Constraints brief) and writes a checkpoint. Summarizing the loaded view keeps handoffs **cumulative** — a second handoff folds the first summary back in. No-op when nothing new since the last fold.
- **TUI**: `:handoff` runs it (pushes a checkpoint block); `:context` toggles the **context viewer** in the side pane (`cli/presentation/contextView.ts` — `buildContextView` partitions `list` + `listCheckpoints` into *archived* segments (folded, not loaded) and the *loaded* segment; `buildContextRows` groups each segment's messages into **foldable, selectable turns**, rendered as a tree so the replacement is visible); `:browse` lists workspace conversations and `:resume <#|id>` switches to one (full replay for browsing, active-window for execution). On boot with no `--resume`, if the workspace has prior conversations, a **startup picker** (`openConversationPicker` → a select overlay, purpose `conversation`) floats "Resume a conversation" over the fresh session — a leading "＋ Start a new conversation" row then `<date> · <title|first-prompt>` per conversation (the title is generated on the utility model after a session's first exchange — `generateSessionTitle` → `ConversationStore.setTitle`); Enter resumes via `resumeConversation`, Esc / "start new" leaves the fresh session. No prior conversations → straight to an empty rail.
- **Curate context → new session**: in the context viewer, `Space` selects/deselects a **turn** *or* a **handoff** (the `⚑` archived-segment header) and `b` (or `:build`) **builds a new conversation** seeded with only the picked units, switching to it (`messagesForSelectedTurns` → `ConversationStore.create`/`append` → the `:resume` switch). A selected handoff contributes **only its summary, as one `handoffToMessage` message** (not the folded originals); a handoff and its own inner turns are therefore **mutually exclusive** (selecting one clears the other — `archivedTurnRanges`/`handoffOwningTurn` in `contextView.ts`). Turn granularity keeps tool-call/result pairs valid; the original conversation is untouched.

## Context compaction (compression without breaking cache)

Cache-safe context compression, implemented natively in `core/usecases/compaction.ts`. The governing constraint: provider prompt caches key on a **byte-stable prefix**, so compression must never rewrite history. Four tactics:

1. **Append-time tool-result compression** (`compressToolResults`, called by `runAgentLoop` on each step's new tail): any string inside a tool-result output over the budget (`Settings.toolResultMaxTokens`, default 4000 tokens ≈ 16k chars, 0 = off; threaded to sub-agent loops via `RunContext` like the step cap) is compressed the moment it enters the buffer — the persisted record and every future prompt carry the compressed form from byte one, and nothing already sent is ever rewritten. Compression is **structure-aware first** (`compactionContent.ts`, the SearchCompressor/LogCompressor ideas from compaction ported to our shapes): grep-shaped output (`path:NN:` lines, any tool) is grouped per file — every file visible, first 5 matches each, exact counts in the marker; Bash output with error signal keeps head/tail + every error with its trace block intact (a state machine absorbs `at `/`File "`/indented continuations) + deduped warnings (`(×N)`) + test-runner summary lines, re-emitted in original order with inline `[…N lines omitted…]` gap markers. Shapeless text falls back to the blind head (75%) + tail (12.5%) clip. **The TUI still shows the full output** (hooks emit the raw response before the tail is compressed).
2. **Reversible markers** — the clip marker names what was dropped (`[…compaction: ~4509 tokens of this Bash output omitted…]`) and how to get it back (re-read with offset/limit, narrower grep, `| head`) — compression the model can undo on demand. Live-verified: the model greps its way to the buried line instead of trusting the hole.
3. **FAST-tier middle digests** — when `UtilityLlm` is in context and the dropped middle is big enough, the fast role writes a ≤120-word summary into the marker (the fast tier's first consumer). Usage is reported via the new `onHelperUsage` hook → `helper_usage` event → `byRole.fast`. Best-effort: no service / provider error degrades to the plain marker; sub-agent loops forward the spend to the parent ledger.
4. **Threshold auto-fold** (`autoHandoffPct`, default 85, 0 = off): when a finished turn's context crossed the threshold, the TUI runs `:handoff` automatically at the boundary — one deliberate prefix rebuild (the only cache-safe way to shrink history), then the cache is warm again. Skipped while messages are queued and on `~` resume estimates.

Note: `read_file`/`web_fetch` carry their own per-tool caps (`truncated: true`); compaction is the *generic backstop* for anything without one — bash stdout, grep floods, future tools. Verified live: an 80k-char `cat` persisted at 14.6k chars with marker + digest, `Σ` showing all three tiers.

## CLI shape

Composition root: `packages/code/src/main.ts`. Four modes under `packages/code/src/modes/`:

- **`tui.ts`** — now just the `TuiModeInput` seam; the driver lives in **`packages/code/src/cli/`** (OpenTUI native renderer + **SolidJS** signals, **no React**), loaded by `main.ts` via a lazy dynamic `import()` so `@opentui/core`'s native FFI lib is touched only on the TUI path. Default in a TTY. **Borderless, single-region** (agy) layout: a one-line **header bar** (`view/chrome/Header.tsx` — `▌efferent` wordmark + the live agent state machine: spinner + phase/tool + elapsed + the `◆ N agents` fleet chip; see Token & model display) above ONE **borderless message region** — the conversation, or a focus-gated contextual **panel** (activity/context/agents/sessions) in its place; no pane borders, no sidebar column, the focus cue is the input-fence accent + the cursor tint. Below the region is the **agy bottom chrome** (replacing the old keybind box), top→bottom: the **running loader** (`view/chrome/RunningLoader.tsx` — a `⣻ thinking 4s` spinner line, the live agent-state machine, shown directly above the input while a turn is in flight; the in-rail "thinking…" indicator was retired in its favour), a pending-queue `▸` list (`view/panes/QueuedMessages.tsx` — messages typed while a turn runs queue here and run as the next turn, agy-style; `↑` on an empty composer pulls the last back to edit), the **input fence** (`view/ui/InputFence.tsx` — a `> ` prompt between two full-width rules that tint to the input accent when focused, no box; the `> ` caret itself **recolours by composer mode** via `composerMode` → message keeps the input accent, a `:command` line turns it the side/overlay accent, a `/search` line the conversation accent — "the command palette replaces the caret"), the `:` command menu / `/` search-status line rendered **below** the fence (agy contextual menus), and the **two-zone status bar** — a left contextual hint (`statusHint`: `? for shortcuts` / `esc to cancel` / `↑ to edit queued` / a live note) and a right readout (`model [· role ids] · gauge% · cache% · storage · cwd`). Keybind discovery is the **`?` shortcuts overlay** (`view/overlays/Shortcuts.tsx` + `presentation/shortcuts.ts`; also `:shortcuts`/`:keys`). The multi-line **`<textarea>`** input (**Enter sends** · Shift-Enter/Ctrl-J insert a newline, Alt-Enter also sends · grows 1→8 rows; in `:`/`​/` mode **Enter runs** the command/search outright and **⇥/→** completes the highlighted palette entry; **↑/↓** recall sent-message history on a single-line message, pull the last queued message, or move the palette highlight in command mode — `dispatch.ts:inputKey` claims these *before* the textarea via `KeyEvent.preventDefault`); the `:` command menu (`:exit`, `:clear`, `:cwd`, `:shortcuts`, `:keys`, `:handoff`, `:context`, `:tree`, `:build`, `:browse`, `:resume`, `:settings`, `:set`, `:model`, `:effort`, `:search`, `:theme`, `:login`, `:logout`, `:onboarding`, `:db`, `:traces`, `:dashboard`) and `/` per-pane search both render below the fence. **Every contextual surface renders borderless inline** in the bottom chrome — no floating modal, no `Overlay` host: the pickers (`:model`/`:theme`/`:effort`/`:search`/`:login`/`:logout`/`:browse`), the `:settings` table, the `?` shortcuts card, the login flow, and the bash-approval sheet (`Sheet`/`BottomMenu` primitives). `:onboarding` is the lone full-screen takeover. `:logout` (no arg) and `:set` (no arg) are menu front doors — a logged-in-provider picker and the settings table. Every list/menu/picker shares one `MenuRow` atom (`glyph.pointer` caret + `KeyHints` footer), so a row/caret change propagates app-wide. Three focus targets (conversation / panel / input) swapped with **Ctrl-h/j/k/l or Ctrl-arrows** (`v` cycles the panel views); the input is INSERT, the read-only conversation + panel NORMAL with a **vim-style fold cursor** (a shared `presentation/paneNav.ts` flattens each to navigable rows; `{`/`}` step paragraphs, `[`/`]` jump messages/heads, `gg`/`G` ends, `⇥`/`↵`/`h`/`l`·←/→ fold the unit under the cursor — **charwise motions `w/b/e` + VISUAL remain deferred**, see roadmap). Conversation pane: `j/k`·↑↓ **scroll lines** + `Ctrl-D/U`·PgUp/PgDn page (a registered `ConvScroller`), while the fold cursor (`{}`/`[]`/`gg`/`G`) tints the current unit + scrolls it in; `Z` fold-all; `/` search (**row-granular** — turn heads, body blocks, tool groups (`searchConversation` in `presentation/conversation.ts`); every match row gets a bg tint (`tokens.match.line` / `currentLine` for the `[i/N]` one) **plus hlsearch-style WORD chips** — each occurrence of the query in plain-text rows renders inverted via `<span style>` segments (`splitByMatch` → the `HlText` primitive in `view/ui/atoms.tsx`; `tokens.match.word`/`wordCurrent` — accent bg, darkest surface fg; markdown prose can't splice spans, so prose CONTAINING the query renders as chip-capable plain text while the search is active — raw markers may show; Esc restores the styled markdown); a folded turn hiding a match tints its header, and jumping **auto-reveals** the match — the containing turn unfolds, a containing tool group expands, vim-`zv`-style (opened folds stay open); + n/N cycle + Esc); and **`y`** yanks the OpenTUI mouse selection via OSC 52 (drag-select is native — `useMouse:true`). The conversation is a Claude-style **event rail**: assistant prose (rendered as **markdown** via OpenTUI's native `<markdown>` — headings/bold/italic/inline-code/lists/links styled, markers concealed; the `●` floats over the top-left via `position:absolute` so the markdown stretches + wraps with a 2-space hanging indent) and each tool lead with a `●` (colour carries run/ok/error), tool headers read `ToolName(arg)`, results hang under a `⎿` connector (`edit_file` diffs render through the native `<diff>` — `+/-` line colouring + line numbers, parsing the canonical unified diff core emits). Fenced **code blocks** and diff **hunks** are **syntax-highlighted** via tree-sitter (`view/syntax.ts` — one combined `SyntaxStyle` carrying both the markdown `markup.*` scopes *and* code capture scopes (`keyword`/`string`/…); the shared `getTreeSitterClient()` singleton + a per-diff `filetype` from its `+++` header; grammars cover **JS/TS/markdown/zig**, others render un-highlighted; the worker is **best-effort** and `runtime.ts` destroys it on exit so Bun can quit). Foldable **turns** (the user line is a quiet `❯ <subject>`; folded → trailing `▸ N steps`). **Sending a message can fold all previous turns** so the live one is the only expanded story — opt-in via `:set autoCollapse on` (`Settings.autoCollapse`, default OFF: turns stay as you left them; the Activity pane's fold-previous-runs rhythm is unconditional). A run of ≥2 tool calls in a turn **aggregates into one summary line** (expanded with live pills WHILE any call runs — `toolGroupExpanded` — settling collapsed when the last lands) — `▸ read · grep · edit  (3 tools, +5 -2)` (verbs with consecutive repeats as `read ×3`, the call count, the rolled-up edit diffstat, plus any running/failed counts; the caret coloured by the group's aggregate state so a failure shows through the fold) — `⇥`/`↵` expands it to the individual pills. Group fold uses **inverse polarity** to a turn: membership in the `collapsed` set ⇒ *expanded* (`toolGroupSummary`/`toolGroupState` in `conversation.ts`; `Z` honours both defaults via `foldIdsByKind`). Every rail block (prose · pill · group · info) is separated by a **blank line** (`marginTop:1` per body item) so the pane breathes. The default side view is **activity** — a dashboard: context gauge + cumulative stats up top (plus the agent's **working plan** — the latest `update_plan` checklist, ✓/●/○ rows pinned under the stats, rebuilt from history on switch like everything else), then the **run tree** (rebuilt from the persisted messages on every context switch — `projectHistory` in `presentation/historyProjection.ts` derives rail blocks + activity tree + diffstat from ONE walk, so resume/build/fork/boot always show the loaded session's own activity, runs folded; live events keep extending it mid-run) (each user message opens a `run` container — `❯ <prompt>`, same quiet styling as the rail — with its turns → tools → sub-agents nested under it; `onRunStart`/`onRunEnd` in `executionTree.ts`, driven by `submit.ts`), with the **workspace sections** (files diffstat, foldable skills/instructions) **pinned at the pane bottom behind a `── workspace ──` rule**; navigable with one **block cursor** across tree + sections (`j/k`·`{}` step rows, `[]` jump heads, `gg/G` ends, `Tab`/`Enter`/`h`/`l`·←/→ fold; `/` searches the rows). The current run stays expanded while it streams; **sending the next message folds the previous runs to one line each**. The **context viewer** (`:context`) is a navigable tree of foldable, selectable **turns and handoffs**: `j/k`·`{}`/`[]` move, `Tab`/`h`/`l`·←/→ fold, `Enter` jumps the conversation, `Space` selects, `b`/`:build` builds a new session from the selection, `/` searches. The **agent navigation pane** (`:tree`, tab `agents`) is the third side view — workspace conversations + their persisted sub-agent trees with git-graph rails (see Sub-agents & context tree): same motions, `↵` switches the active session / previews a node, `c` forks a node into a new session, `d` drops a node + descendants. **Ctrl-C** is 2×-to-quit. Structure under `packages/code/src/cli/`: `runtime.ts` (composition root + the three-runtime bridge), `state/` (signal slices), `events/` (Effect→signal pump), `actions/` (signal→Effect), `keys/` + `commands/`, `view/` (`panes/`, `panes/side/`, `chrome/`, `overlays/`, and `ui/` — the token-driven primitives `Pane` (a borderless container)/`Sheet`/`BottomMenu`/`InputFence`/`Rule`/`Cursor`/`Marker`/`RailLine`/`SectionHead`, the only components that paint surfaces/glyphs), and `presentation/` — the **pure L1** model/state layer (`theme/` (the two-tier **design system**: `palette` → semantic `tokens` + `glyph` + `themes`; a theme is one complete set of token values; **`presentation/theme/` is pure + static**, while `state/theme.ts` wraps the active theme in a process-global Solid signal exposing a **Proxy-backed reactive `tokens`** every view reads, so **`:theme`** switches live with no call-site change — ships **`efferent`** (the default — warm near-black, ember/verdigris/chartreuse accent triad) + `one-dark` + `tokyo-night`, the choice persisted to `config.json` (`Settings.theme`) + seeded at boot; `view/syntax.ts` memoises its `SyntaxStyle` per theme name so code highlighting follows the switch; **no raw hex/glyph literal lives outside `theme/`**), `conversation` (rail block/turn/fold model), `contextView`, `sidePane`, `selectBox`, `promptBox`, `loginFlow`, `settingsView`, `slashPalette`, `statusBar`, `executionTree`, `toolDescribe`, `dbStatus`, `logger`); no Solid/OpenTUI imports. (This was the old sibling `tui/` folder + `model/conversation.ts`, renamed so the tree no longer reads as two TUIs.) `terminal` (OSC/spinner/ANSI consts) is shared infra at `src/terminal.ts` — a non-TUI mode imports it.
- **`print.ts`** — one-shot. Prompt from argv or stdin (`-`). Final text on stdout, tool log on stderr. Exits when done.
- **`json.ts`** — same control flow as print, but every `AgentEvent` is JSONL on stdout.
- **`rpc.ts`** — bidirectional JSON-RPC on stdin/stdout. Method: `agent.send({ prompt, conversationId? })` → emits `agent.event` notifications, resolves with `{ conversationId, finalText }`.

Modes share one `AgentEvent` union (`packages/code/src/events.ts`); the loop's hooks push events onto a queue; each mode renders differently.

## Models & providers (multi-provider router)

The agent loop talks to one provider-agnostic `LanguageModel`; which provider/model backs it is a **runtime selection**, not a compile-time layer choice.

- **Router** (`adapters/src/llm/router.ts`, `RouterLanguageModelLive`): a `LanguageModel` whose `generateText`/`streamText`/`generateObject` read `ModelRegistry.current` on every call, resolve a key from the `AuthStore` (refreshing an expired OAuth token first), and build the chosen provider's `@effect/ai` service **per request** from that key over a shared `FetchHttpClient` (`Effect.scoped`/`unwrapScoped` so the client lives exactly one call). So a credential added mid-session via `:login`, or a `/model` switch, takes effect on the next turn with no rebuild. There is no longer a `clients.ts` — keys aren't captured at layer-build.
- **Anthropic OAuth** (subscription): when the credential is `oauth`, the client is built with `apiKey: undefined` + a `transformClient` that sends `Authorization: Bearer …` and `anthropic-beta: claude-code-20250219,oauth-2025-04-20` (no `x-api-key`), and the router prepends the required `"You are Claude Code, Anthropic's official CLI for Claude."` system block (`Prompt.merge`). Protocol + constants in `adapters/src/auth/oauth/anthropic.ts` (PKCE, authorize URL, exchange, refresh); callback server + browser-open in `cli/src/login/oauthServer.ts`.
- **Selection** lives in `SettingsStore` as `settings.model = "<provider>:<modelId>"` (the single source of truth, persisted to `.efferent/config.json`). `parseModel`/`formatModel`/`contextWindowFor`/`defaultModelForProvider(s)` are pure helpers in `@xandreed/sdk-core/entities/Model.ts`.
- **`ModelRegistry`** port (`@xandreed/sdk-core/ports/ModelRegistry.ts`): `current` (parsed selection), `list` (live catalogue), `select` (persist + return). `ModelRegistryLive` (`adapters/src/llm/modelRegistry.ts`) fetches the catalogue over **raw HTTP** (Google `…/v1beta/models`, OpenAI `…/v1/models`, Anthropic `…/v1/models`) and parses defensively; keys come from the `AuthStore`, so only logged-in providers are queried (Anthropic OAuth uses a Bearer header).
- **`ModelLive`** bundles router + `ModelRegistry` + dynamic `LlmInfo` over an internal `FetchHttpClient`; requires `SettingsStore` + `AuthStore`. Replaces the old single-provider `GoogleLive`.

**Model roles (main / fast)** — one mental model, two knobs (`ModelRole` + `modelForRole`/`roleIsConfigured`/`selectionFromString` in `entities/Model.ts`). **All agentic work runs on main** — the root conversation AND spawned sub-agents (delegation changes the context, not the brain); the non-main role exists for one-shot helper calls, reached through `UtilityLlm.complete(prompt, { role: "fast" })`:

- **main** — the root conversation's model (`settings.model`, the `/model` switch). Sub-agents inherit it.
- **fast** — helper calls reached off the main loop (`settings.fastModel`; unset → main): compaction middle digests (see Context compaction), auto-approval judgments (see Agent loop → Bash safety), and **session titles** (named after the first exchange, persisted via `ConversationStore.setTitle`, shown in the sessions pane/picker/`:browse` and as the conversation pane's title) — anything where a round-trip on main would drag the run.

`UtilityLlmLive` resolves the requested role per call (configured selection else the live main selection) and returns `{ text, usage }` so each tier's spend is countable. Per-role billed tokens accumulate in `SessionStats.byRole` (root + sub-agent usage → main; fast helper usage → fast) and render as the Activity ledger `Σ main 64k · fast 160`.

**`/model`** (TUI): no arg opens the live-catalogue picker for **main** (persists, updates the status bar); **`:model fast`** opens the same picker for the fast helper role, with a leading *default (follow main)* row to clear it. Direct path: `:set fastModel <provider>:<modelId>|default`. Switching provider mid-conversation applies going forward and surfaces a one-line hint (Gemini 400s on prior non-Gemini tool-calls that lack a `thought_signature` — `/reset` if it errors).

**Caching** is aggressive but provider-native: OpenAI gets automatic prompt-prefix caching + a stable `prompt_cache_key`; Gemini relies on implicit context caching (stable prefix → `cachedContentTokenCount`); **Anthropic requires explicit opt-in** — the router stamps `cache_control: ephemeral` breakpoints (`withAnthropicCacheBreakpoints` in `providers.ts`) on the last system message + the last two non-system messages every call (markers move with the tail; placement isn't part of Anthropic's content hash, so old prefixes keep hitting). Helper tiers (UtilityLlm, web search) deliberately don't stamp — one-shot prompts would pay the +25% write premium for nothing. Anthropic's `input_tokens` EXCLUDES cache reads/writes, so `extractUsage` folds the raw anthropic metadata back in (else the gauge reads ~0 on a cached turn). Live-verified: turn 2 on Sonnet = `93% cached`. Explicit Gemini `cachedContent` is not expressible through `@effect/ai-google` today (it always sends full `contents`), so we don't fake it.

## Token & model display

`LlmInfo.metadata` (provided by `ModelLive`, following the live `ModelRegistry.current`) exposes `{ modelId, contextWindow }`; the TUI status bar reads it at startup and `/model` updates it on switch. After each turn, `onAssistantMessage` carries a `TokenUsage` (input / output / total / cacheRead) for the gauge; `cacheReadTokens` comes from Gemini's `usageMetadata.cachedContentTokenCount`.

The **status bar** reads `model [effort] [· fast <id>] · gauge 12% 18k/1M · 86% cached · storage · cwd`: the roles readout (`rolesChip`) shows the configured fast role's model id, the gauge percent + colour follow `gaugeSeverity` (muted → warn at 70% → critical at 90%, when an explicit `:handoff to fold` nudge appears), and `cachePercent` is the share of the last turn's input served from the provider cache. The Activity header carries the same gauge + the `Σ main/fast` spend ledger. The **header bar** above the panes (`view/chrome/Header.tsx`) is the agent's face: `▌efferent  ⠹ Read(main.ts) 4s  ◆ 2 agents · audit state, fix scroll` — a pure state machine (`presentation/agentState.ts`: idle → thinking → tool, with the live sub-agent FLEET orthogonal) fed by the event pump; the rail shows a `thinking…` spinner during the model-latency gap.

**Context window** comes from `contextWindowFor(provider, modelId)` (`entities/Model.ts`), which reads a **generated catalogue** (`entities/modelCatalog.generated.ts`, snapshotted from [models.dev](https://models.dev) by `bun run generate-models` → `scripts/generateModelCatalog.ts`) — neither Anthropic's nor OpenAI's `/models` API reports a context length, so the catalogue is the source of truth (e.g. Sonnet 4.6 / Opus 4.6+ = 1M). A bare per-provider substring heuristic remains as the fallback for ids newer than the last snapshot; dated ids (`…-20260101`) fall back to their base id. Google's live `inputTokenLimit` still wins when present.

## Skills

`.efferent/skills/*.md` files are auto-discovered at startup. The search path walks `cwd → parents → ~/.efferent/skills/`; closer-to-cwd shadows farther on name collisions. Each skill file has YAML-ish frontmatter and a free-form markdown body:

```
---
name: <slug>
description: <one-line summary for the prompt>
---

(detailed procedure for the agent to follow)
```

At startup, names + descriptions are injected into the coder system prompt under a `# Skills` section. The bodies are lazy-loaded by the model via `read_skill({ name })` only when relevant — reusable procedures you can ship without changing the code.

Loader: `loadSkills(cwd, homeDir)` in `@xandreed/sdk-core/usecases/loadSkills.ts`. Failures (missing dirs, malformed frontmatter) are silently skipped — a broken skill never breaks the agent.

## Evals

A fourth package, **`packages/evals`** — a minimal, Effect-native eval library: the `data → task → scorers` shape expressed as Effects, so a `task` can be the real agent loop and a `Scorer` can itself call an LLM. No sqlite, no UI, no persistence; runs under Bun directly.

- **Framework** (`src/framework/`): `EvalSpec<I,O,T,R>` is pure data (`defineEval`); `runEval(spec)` returns `Effect<EvalReport, never, R>` — every per-case/per-scorer failure is captured via `Effect.exit` (so a provider 429 scores 0, never crashes the run) and `R` is left open for the caller to provide once. Scorers (`scorers.ts`): `predicate`, `includesAll` (substring-coverage ratio), `fromEffect`, and `llmJudge` (LLM-as-judge, parses `{"score","reason"}`). `report.ts` prints a coloured per-suite table.
- **Env** (`src/env.ts`): `EvalEnvLive` mirrors `main.ts`'s composition but swaps Postgres for an in-memory `ConversationStore` (`support/inMemoryConversationStore.ts`, replicating the position/checkpoint fold semantics) so evals need no Docker. `support/workspace.ts` gives `withTempWorkspace` (acquire/release temp dirs); `support/coder.ts` (`runCoder`) stands up a real coder agent over a temp repo and reports the tools it called + final text + read-back files.
- **Config injection** (`src/config/`): a `RunConfig` pins `{main, fast, judge, promptVariant, maxSteps}`; `makeEvalEnv(config?)` swaps in a `FixedSettingsStoreLive` (its `load()` ignores disk) so a `--config matrix.json` run is the independent variable A/B'd against a baseline. No config ⇒ today's `LocalSettingsStoreLive`.
- **Suites** (`src/suites/*.eval.ts`): `handoff`, `tool-selection`, `coder-edit` (originals) + `whole-task` (bug-fix / multi-file / refactor / failing-test / read-only Q&A through the FULL loop), and three **fast-model** suites calling the fast-tier use cases directly — `judge-approval` (labelled allow/prompt accuracy), `compaction-digest` (`compressToolResults` fidelity), `session-title` (`generateSessionTitle` quality).
- **Run**: `bun run eval [name …] [--config f] [--main m] [--fast m] [--json]` (`src/run.ts`). Gated on `hasKey` — no key → skip cleanly. Each run is collected **in-memory** and the report is **built from the spans** (`trace/process.ts` → `trace/report.ts`); set `OTEL_EXPORTER_OTLP_ENDPOINT` and it also streams to Grafana. Unit tests (`bun test`) cover the framework, the store fold contract, and the pure trace processor (synthetic spans), with no LLM/Docker.

## Observability (traces + metrics)

Trace-first, by design: **production code only annotates** OpenTelemetry spans and records a few Effect `Metric`s — it never builds a metrics store. The waterfall is the full execution: `agent.run` (one per **user message** — carries `agent.conversation_id`, an `agent.prompt` excerpt, `agent.model`, run-total tokens) → `agent.turn` → `llm.generate` (model + `gen_ai.usage.*` + `gen_ai.cost_usd` + `gen_ai.cache_hit_ratio` + finish reason, plus the clipped `gen_ai.prompt`/`gen_ai.completion` text — captured whenever telemetry is on, no separate flag) → **`agent.tool`** (one per tool call — `agent.tool.name` / `.ok` / `.args_summary`, the seam in `recoverMalformedToolCalls`) and **`agent.subagent`** (a spawned run's subtree — node/depth/folder), plus the helper spans `agent.approval.judge` / `agent.compaction.digest` / `agent.title`. Metrics (`core/src/telemetry/metrics.ts`): `gen_ai_tokens_total`, `gen_ai_calls_total`, **`gen_ai_cost_usd_total`** (priced via the same `costUsd` catalogue the evals use), `agent_turns_total`, `agent_turn_latency_ms`, `agent_tool_calls_total`, **`agent_errors_total{kind=run|turn|tool|llm,error}`** (RED's E), `approval_verdicts_total`. **Bounded tags only** — conversation/node/run id + prompt live on **spans**, never on a metric. Everything is **inert without a tracer** — zero overhead un-exported.

- **Export** is one layer at the edge: `OtlpTelemetryLive` (`adapters/src/telemetry/otlp.ts`, Effect-native `Otlp.layerJson` over HTTP — no heavy OTel SDK in the CLI bundle). It's wired through a `Layer.unwrapEffect` in `main.ts` that reads the **`Settings.telemetry`** flag (the SOLE switch, `:set telemetry on`; schema default off) and returns `OtlpTelemetryLive` or `Layer.empty` — no env var enables export. Resource: `service.name=efferent` + `deployment.environment=production`; the endpoint defaults to `http://localhost:4318` (`OTEL_EXPORTER_OTLP_ENDPOINT` overrides WHERE, not WHETHER).
- **Eval / prod separation** — one stack, logical split. Evals export under `service.name=efferent-evals` (→ Prometheus `job`) + `deployment.environment=eval` + a `resource.eval.run_id`, into a **separate Grafana folder**; prod dashboards filter `{job="efferent"}`, eval dashboards `{job="efferent-evals"}`. Evals still **build their report FROM in-memory spans** (`trace/process.ts`) and only ship to the stack when `OTEL_EXPORTER_OTLP_ENDPOINT` is set.
- **Grafana dashboards** (`grafana/otel-lgtm`; auto-provisioned from `observability/grafana/dashboards/{production,evals}/` via two folder providers). Production: **fleet health** (`efferent-fleet` — RED + cost $ + cache-hit + tool success, SI/`$`/`%` units), **conversations** (`efferent-conversations` — Tempo table, one row per user-message trace, data-links into) **conversation** (`efferent-conversation?var-conversation=…` — that session's messages + tool calls → click a trace for the waterfall). Evals: **eval runs** (`efferent-evals` — per-suite mean/pass/cost + a `$run`-scoped case-trace table). The CLI deep-links out: **`:traces`** (active conversation) / **`:dashboard`** (fleet), Grafana base from `Settings.grafanaUrl` (default `http://localhost:3000`); `bun run eval` prints an eval-dashboard link when exporting. The required panels are Prometheus + Tempo-search; TraceQL-metrics panels are fail-soft enhancements. Full runbook + caveats in `observability/README.md`.

## Deferred (do not build until they hurt)

See `docs/roadmap.md` for the full backlog. Highlights of what's NOT yet built:

- **Explicit Gemini context caching** — implicit caching is live (see Models & providers). Explicit `cachedContent` resources would need `@effect/ai-google` to let us send a trimmed `contents` + suppress system/tools; it can't today, so deferred. Cost optimisation, not correctness.
- **Approval beyond bash** — the `Approval` port + TUI modal ship for bash (see Agent loop → Bash safety); extending the same rule-keyed consent to out-of-cwd writes and network egress is the follow-up, as is a `:approvals` view to list/revoke persisted project rules.
- **Live token streaming** — the loop uses `generateText` per turn; switch to `streamText` and map stream parts to events for token-level TUI updates.
- **Per-conversation OpenAI `prompt_cache_key`** — currently a stable static key; threading the conversation id would tighten cache routing.

- **Settings UI / config files** — a few knobs still hardcoded in `main.ts` composition (`/model` + `/set` cover model + the core settings).
- **Mid-run auto-compaction** — between-run auto-folding SHIPS (`autoHandoffPct`, see Context compaction); folding *inside* a long run via `onTransformContext` (the hook is wired but unused) is the remaining piece; needs the loop's tail-persistence to be robust to a shrinking buffer first.
- **Streaming tool output** — bash stdout chunks back to the model live.
- ~~Human-driven re-run from `:tree`~~ — SHIPPED: `↵` switch/preview, `c` fork, `d` drop, and typing in an open preview resumes the node in place (`ScopeRuntime.resumeNode`).
- **Conversation-level branch / fork** (the *agent* context tree ships; forking the top-level conversation itself is still `:context` + `:build` curation); **extension system**; **parallel tool execution**.
- **Image attachments** in the CLI; **native (non-shell-out) grep**.
- **Charwise vim motions + VISUAL in the TUI** — the read-only panes now ship a **fold cursor** (a row-granular cursor over the shared `presentation/paneNav.ts` rows: `{}`/`[]`/`gg`/`G` move, `⇥`/`↵`/`h`/`l` fold the unit under it) **and per-pane `/` search**. What's still deferred is a **charwise per-line cursor** + NORMAL/VISUAL motions (`w/b/e`, `0/^/$`, charwise/linewise VISUAL) layered on the panes + the `<textarea>` (reuse the pure `viMode` logic deleted with the old driver — recover from git history). Related deferred TUI bits: **wider code-highlight language coverage** (markdown prose + fenced-block + diff-hunk syntax highlighting all ship via tree-sitter, but `@opentui/core` only bundles **JS/TS/markdown/zig** grammars — python/rust/go/json/bash/etc. render un-highlighted until we `addFiletypeParser` more grammar WASM + queries; also still unstyled: `#` heading *colour*, since marked headings route around the `markup.heading` scope which only styles table headers), **simultaneous match highlight for side-pane search** (the cursor + `[i/N]` counter mark the current hit; other matches aren't tinted), **prompt-history recall** on ↑/↓ (collides with the textarea's line navigation — intercept at the first/last line), **Ctrl-R tool-output expand**, the **Shift-Tab effort shortcut**, and **per-tool detail folding** (turn + tool-group folding is per-section today).
- **Evals**: the `packages/evals` harness ships (see Evals). Deferred *within* it: result persistence / trend tracking across runs, an `onAfterToolCall`-driven trajectory scorer, dataset files (cases are inline today), CI wiring. Telemetry + structured logging beyond what `Effect.log` already prints.

## OPSEC reminder

Every commit under this tree must be authored as `Xand Reed <xandreed@proton.me>`. Verify with `git config user.email`. Parent CLAUDE.md is non-negotiable on this — never reference the real name in any file, commit, comment, or screenshot. Never commit anything from `~/Workspace/xandreed/pi` — it's read-only research material.
